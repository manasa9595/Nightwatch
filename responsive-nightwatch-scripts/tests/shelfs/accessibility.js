/*var props = require('../components/other_comp/accessibility_props.js');
module.exports = {
    "Focus on breadcrumb --": function(client) {
        var pageObj = client.page.accessibility_selectors(),
            signup = client.page.signup(),
            callback = client.page.common(),
            shelfCls = pageObj.elements.shelfSel.selector;
            
            client.url(signup.url);
            client.waitForElementVisible("body",5000);
            client.maximizeWindow();
      
    }
}
*/
