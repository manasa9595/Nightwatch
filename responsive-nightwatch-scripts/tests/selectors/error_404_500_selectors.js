module.exports = {
    url: 'https://rbs-retail-test-61.adobecqms.net/responsive_personal/500page.html',
    elements: {
    	ShelfPadding : {
    		selector : '.message-only' 
    	},
    	imgMargin : {
    		selector : '.message-only .message-only__image-wrapper'
    	},
    	imgSize : {
    		selector : '.message-only .message-only__image-wrapper img'
    	},
    	titleMargin : {
    		selector : '.message-only .message-only__heading'
    	},
    	shelfTitle : {
    		selector : '.message-only .message-only__heading .title-comp'
    	},
    	searchMargin : {
    		selector : '.message-only .message-only__search'
    	},
    	shelfText : {
    		selector : '.comp-rich-text'
    	},
    	searchPadding : {
    		selector : '.message-only .message-only__search .taskpanel__search__input-wrap'
    	},
    	searchText : {
    		selector : '.message-only .message-only__search .taskpanel__search__input'
    	},
        messageOnlyHeader : {
            selector : '.logo__header'
        },
        messageOnlyHeaderImg : {
            selector : '.logo__header img'
        }

    } 
}